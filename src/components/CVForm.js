import React from "react";
import styled from 'styled-components';

const StyledForm = styled.section`
margin: 0 1vw;
`

export default function CVForm(props) {
    return (
        <StyledForm style={{overflow: "hidden", backgroundColor: "blue"}}>
            Form
        </StyledForm>
    )
}